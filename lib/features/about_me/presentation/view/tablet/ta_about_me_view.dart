import 'package:flutter/material.dart';

class TaAboutMeView extends StatelessWidget {
  const TaAboutMeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AboutMe View - Tablet'),
    );
  }
}