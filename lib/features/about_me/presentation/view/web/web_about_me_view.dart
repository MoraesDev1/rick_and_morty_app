import 'package:flutter/material.dart';

class WebAboutMeView extends StatelessWidget {
  const WebAboutMeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('About Me View - Web'),
    );
  }
}