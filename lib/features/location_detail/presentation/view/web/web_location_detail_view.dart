import 'package:flutter/material.dart';

class WebLocationDetailView extends StatelessWidget {
  const WebLocationDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Location View - Web'),
    );
  }
}