import 'package:flutter/material.dart';

class TaLocationDetailView extends StatelessWidget {
  const TaLocationDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Location View - Tablet'),
    );
  }
}