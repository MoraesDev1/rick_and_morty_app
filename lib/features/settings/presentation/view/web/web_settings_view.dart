import 'package:flutter/material.dart';

class WebSettingsView extends StatelessWidget {
  const WebSettingsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Settings View - Web'),
    );
  }
}