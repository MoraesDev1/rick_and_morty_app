import 'package:flutter/material.dart';

class TaSettingsView extends StatelessWidget {
  const TaSettingsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Settings View - Tablet'),
    );
  }
}