import 'package:flutter/material.dart';

class TaCharacterDetailView extends StatelessWidget {
  const TaCharacterDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Home View - Tablet'),
    );
  }
}