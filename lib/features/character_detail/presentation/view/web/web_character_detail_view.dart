import 'package:flutter/material.dart';

class WebCharacterDetailView extends StatelessWidget {
  const WebCharacterDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Home View - Web'),
    );
  }
}