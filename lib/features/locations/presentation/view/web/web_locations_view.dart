import 'package:flutter/material.dart';

class WebLocationsView extends StatelessWidget {
  const WebLocationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Locations View - Web'),
    );
  }
}