import 'package:flutter/material.dart';

class TaLocationsView extends StatelessWidget {
  const TaLocationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Locations View - Tablet'),
    );
  }
}