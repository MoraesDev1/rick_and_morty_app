import 'package:flutter/material.dart';

class WebCharactersView extends StatelessWidget {
  const WebCharactersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Characters View - Web'),
    );
  }
}