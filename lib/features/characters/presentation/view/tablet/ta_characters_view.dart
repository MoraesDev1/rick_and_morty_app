import 'package:flutter/material.dart';

class TaCharactersView extends StatelessWidget {
  const TaCharactersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Home View - Tablet'),
    );
  }
}