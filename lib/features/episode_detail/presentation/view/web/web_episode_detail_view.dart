import 'package:flutter/material.dart';

class WebEpisodeDetailView extends StatelessWidget {
  const WebEpisodeDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Episode View - Web'),
    );
  }
}