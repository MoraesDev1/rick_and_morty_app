import 'package:flutter/material.dart';

class WebEpisodesView extends StatelessWidget {
  const WebEpisodesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Episodes View - Web'),
    );
  }
}