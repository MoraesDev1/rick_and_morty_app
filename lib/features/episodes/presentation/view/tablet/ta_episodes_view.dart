import 'package:flutter/material.dart';

class TaEpisodesView extends StatelessWidget {
  const TaEpisodesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Episodes View - Tablet'),
    );
  }
}